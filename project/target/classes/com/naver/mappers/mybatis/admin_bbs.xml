<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="AdminBbs">
	<!-- 검색전후 레코드개수 -->
	<select id="abbs_count" parameterType="bbs" resultType="int"> <!-- resultType은 반환 자료형 -->
		select count(bbs_no) from bbs
		<where>
			<if test="find_field ==''"></if> <!-- 검색전 기준 -->
			<if test="find_field=='bbs_title'"> <!-- 제목기준 -->
				bbs_title like #{find_name} 
			</if>	
			<if test="find_field =='bbs_cont'"> <!-- 내용기준 -->
				bbs_cont like #{find_name}
			</if>
		</where>
	</select>


	<select id="abbs_list" parameterType="bbs" resultType="bbs"> 
		select * from (
		select rowNum rNum, bbs_no, bbs_name, bbs_title, bbs_hit, bbs_ref, bbs_step, bbs_level, bbs_date
		from (select * from bbs
		<where>
			<if test="find_field ==''"></if> <!-- 검색전 기준 -->
			<if test="find_field=='bbs_title'"> <!-- 제목기준 -->
				bbs_title like #{find_name} 
			</if>	
			<if test="find_field =='bbs_cont'"> <!-- 내용기준 -->
				bbs_cont like #{find_name}
			</if>
		</where>
		order by bbs_ref desc, bbs_level asc)) where rNum &gt;= #{startrow} and rNum &lt;= #{endrow}
	</select>
	
		<!-- 관리자  자료실 저장 -->
	<insert id="abbs_in"> 
		insert into bbs (bbs_no,bbs_name, bbs_title, bbs_pwd, bbs_cont, bbs_file, bbs_ref, bbs_step, bbs_level, bbs_date) 
		values (bbs_no_seq.nextval, #{bbs_name}, #{bbs_title} , #{bbs_pwd}, #{bbs_cont}, #{bbs_file}, bbs_no_seq.nextval, 0, 0, sysdate)	</insert>
</mapper>