<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="exboard">
	 <select id="ex_list" resultType="exb">
	 	select * from (select rowNum rnum, b_no, b_writer, b_title, b_hit, regdate from (select * from tbl_boardEx order by b_no desc)) 
	where rnum &gt;= #{startrow} and rnum &lt;=#{endrow}
	 </select>
	 <select id="ex_count" resultType="int">
	 	select count(*) from tbl_boardEx
	 </select>
</mapper>